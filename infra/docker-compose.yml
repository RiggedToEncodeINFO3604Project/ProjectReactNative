version: "3.8"

services:
  web:
    build:
      context: .
      dockerfile: infra/Dockerfile
    ports:
      - "8081:8081"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
